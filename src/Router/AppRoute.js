import { BrowserRouter, Route, Routes } from "react-router-dom";

function AppRoute (){
    return (
        <BrowserRouter>
            <Routes>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
                <Route path="/" element={()=>{}}/>
            </Routes>
        </BrowserRouter>
    );
}
export default AppRoute;